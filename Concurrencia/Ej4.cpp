#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <pthread.h>
#include <mutex>

using namespace std;

//CONSIGNA: El usuario ingresa por pantalla un numero cualquiera. Luego, se deberá crear esa cantidad de threads. Todos los threads ejecutaran el mismo codigo, el cual imprimira por pantalla "A". Finalmente, el hilo principal espera a que finalicen todos los threads.

int main ()
{
	
	
	return 0;
}



//Para compilar: g++ Ej1.cpp -std=c++11 -lpthread 
